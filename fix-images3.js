const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.resolve(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else if (file.endsWith('.js')) { results.push(file); } }); return results; } const files = walk('D:/GG/GG-Managment2026/ancient-kepler Pro/src/js'); files.forEach(file => { let content = fs.readFileSync(file, 'utf8'); let original = content; content = content.replace(/<img=\{Image/g, '<img src={Image'); content = content.replace(/<img=\{Image1/g, '<img src={Image1'); content = content.replace(/<img=\{Image2/g, '<img src={Image2'); content = content.replace(/<img=\{images/g, '<img src={images'); content = content.replace(/<img (.*?)=\{Image/g, '<img  src={Image'); content = content.replace(/<img (.*?)=\{Image1/g, '<img  src={Image1'); content = content.replace(/<img (.*?)=\{Image2/g, '<img  src={Image2'); content = content.replace(/<img (.*?)=\{images/g, '<img  src={images'); if (content !== original) { fs.writeFileSync(file, content); console.log('Fixed ' + file); } }); console.log('Done!');
